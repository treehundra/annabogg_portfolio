A pill chip for hard skills, disciplines and categories. Oswald uppercase.

```jsx
<Tag>Editorial styling</Tag>
<Tag variant="solid">Creative direction</Tag>
<Tag variant="ghost" size="sm">Commercial</Tag>
```

Variants: `outline` (default), `solid` (black), `ghost` (grey). Lay several in a `display:flex; gap` row.
